# Stories CI verification

This marker commit intentionally triggers the existing `CI` workflow on `main` after the Stories implementation. It contains no runtime code and may be removed after verification.
