import { HERO_BLOCKOUT_ASSET_KEY,HERO_BLOCKOUT_REPORT_VERSION,HERO_DNA_MORPH_NAMES,HERO_EXPRESSION_MORPH_NAMES,HERO_COMBINATION_CASE_NAMES,HeroBlockoutReport } from './hero-blockout-report-v12.domain';
import { AVATAR_CANONICAL_SKELETON } from './avatar-asset-manifest.domain';
import { HERO_BLOCKOUT_PROVENANCE_VERSION,HeroBlockoutProvenance,sha256Hex,validateHeroBlockoutProvenance,verifyHeroBlockoutSource } from './hero-blockout-provenance.domain';
const report=():HeroBlockoutReport=>({reportVersion:HERO_BLOCKOUT_REPORT_VERSION,assetKey:HERO_BLOCKOUT_ASSET_KEY,unitSystem:'METERS',authoringUpAxis:'Z',runtimeUpAxis:'Y',pose:'A_POSE',poseVerified:true,measuredLeftUpperArmAngleDeg:40,measuredRightUpperArmAngleDeg:40,centeredWorldOrigin:true,measuredBodyCenterX:0,groundContactY:0,measuredGroundContactMeters:0,groundContactVerified:true,bodyHeightMeters:1.68,skeletonTarget:AVATAR_CANONICAL_SKELETON,stableVertexOrder:true,deformationTopologyReady:true,skinningVerified:true,measuredUnweightedBodyVertices:0,measuredMaxBodyBoneInfluences:4,measuredMaxBodyWeightSumError:.001,uvVerified:true,measuredBodyUvLayers:1,measuredBodyUvOutOfBoundsLoops:0,pbrMaterialsVerified:true,measuredMaterialSlots:3,measuredMaxMaterialsPerObject:1,texturesVerified:true,bodyBaseColorTextureVerified:true,bodyNormalTextureVerified:true,bodyOrmTextureVerified:true,textureColorSpacesVerified:true,measuredTextureCount:3,measuredMaxTextureDimension:2048,measuredTotalTexturePixels:12582912,dnaMorphsVerified:true,dnaMorphNames:[...HERO_DNA_MORPH_NAMES],measuredDnaMorphCount:HERO_DNA_MORPH_NAMES.length,measuredMaxDnaVertexDeltaMeters:.12,expressionsVerified:true,expressionMorphNames:[...HERO_EXPRESSION_MORPH_NAMES],measuredExpressionMorphCount:HERO_EXPRESSION_MORPH_NAMES.length,measuredMaxExpressionVertexDeltaMeters:.04,combinedDeformationsVerified:true,combinationCaseNames:[...HERO_COMBINATION_CASE_NAMES],measuredCombinationCaseCount:HERO_COMBINATION_CASE_NAMES.length,measuredMaxCombinedVertexDeltaMeters:.2,measuredMaxSelfIntersectionCount:0,lodsVerified:true,lodCanonicalArmatureName:'RIG_HUMANOID',lodShapeKeyNames:['Basis',...HERO_DNA_MORPH_NAMES,...HERO_EXPRESSION_MORPH_NAMES],lodMetrics:[{level:0,objectName:'BODY_LOD0',vertices:30000,triangles:45000,maxBonesPerVertex:4,reductionFromPrevious:0},{level:1,objectName:'BODY_LOD1',vertices:18000,triangles:26000,maxBonesPerVertex:4,reductionFromPrevious:Number((1-26000/45000).toFixed(6))},{level:2,objectName:'BODY_LOD2',vertices:7000,triangles:10000,maxBonesPerVertex:4,reductionFromPrevious:Number((1-10000/26000).toFixed(6))}],objects:[{role:'BODY',vertices:30000,triangles:45000,manifold:true,unappliedTransforms:false,fusedClothingOrAccessories:false},{role:'EYE_L',vertices:800,triangles:1400,manifold:true,unappliedTransforms:false,fusedClothingOrAccessories:false},{role:'EYE_R',vertices:800,triangles:1400,manifold:true,unappliedTransforms:false,fusedClothingOrAccessories:false}]});
const bytes=Buffer.from('real-hero-blockout-binary-fixture');
const valid=():HeroBlockoutProvenance=>({version:HERO_BLOCKOUT_PROVENANCE_VERSION,report:report(),sourceFormat:'GLB',sourceSha256:sha256Hex(bytes),exporter:'Blender 4.x KnowMe exporter',exportedAt:'2026-09-18T07:00:00.000Z'});
describe('Hero blockout source provenance gate',()=>{
 it('accepts a valid measured v12 report bound to source bytes',()=>expect(verifyHeroBlockoutSource(bytes,valid())).toBeDefined());
 it('rejects a forged digest',()=>{const x=valid();x.sourceSha256='0'.repeat(64);expect(()=>verifyHeroBlockoutSource(bytes,x)).toThrow(/digest mismatch/);});
 it('revalidates geometry',()=>{const x=valid();x.report.objects[0].manifold=false;expect(()=>validateHeroBlockoutProvenance(x)).toThrow(/non-manifold/);});
 it('revalidates PBR',()=>{const x=valid();x.report.bodyOrmTextureVerified=false as true;expect(()=>validateHeroBlockoutProvenance(x)).toThrow(/PBR texture evidence/);});
 it('revalidates DNA',()=>{const x=valid();x.report.dnaMorphNames[0]='dna_forged';expect(()=>validateHeroBlockoutProvenance(x)).toThrow(/DNA morph evidence/);});
 it('revalidates expressions',()=>{const x=valid();x.report.expressionMorphNames[0]='expr_forged';expect(()=>validateHeroBlockoutProvenance(x)).toThrow(/non-canonical/);});
 it('revalidates combined stress matrix',()=>{const x=valid();x.report.combinationCaseNames[0]='skip_qa';expect(()=>validateHeroBlockoutProvenance(x)).toThrow(/stress matrix/);});
 it('revalidates self-intersection evidence',()=>{const x=valid();x.report.measuredMaxSelfIntersectionCount=1;expect(()=>validateHeroBlockoutProvenance(x)).toThrow(/self-intersections/);});
 it('revalidates LOD reduction',()=>{const x=valid();x.report.lodMetrics[1].triangles=44000;expect(()=>validateHeroBlockoutProvenance(x)).toThrow(/reduction/);});
 it('revalidates LOD skinning',()=>{const x=valid();x.report.lodMetrics[2].maxBonesPerVertex=5;expect(()=>validateHeroBlockoutProvenance(x)).toThrow(/skinning/);});
 it('revalidates LOD shape keys',()=>{const x=valid();x.report.lodShapeKeyNames[1]='dna_forged';expect(()=>validateHeroBlockoutProvenance(x)).toThrow(/shape-key/);});
 it('rejects non-canonical timestamps',()=>{const x=valid();x.exportedAt='2026-09-18 07:00:00';expect(()=>validateHeroBlockoutProvenance(x)).toThrow(/ISO-8601/);});
});
