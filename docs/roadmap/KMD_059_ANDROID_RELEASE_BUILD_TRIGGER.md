# KMD-059 Android standalone release build trigger

This technical-only branch exists to generate a standalone Android APK for real-device KMD-059 validation.

The release APK must embed the JavaScript bundle and start without Metro. It introduces no product, API, schema, permission, entitlement, Nexus, legal, or security-boundary change.
