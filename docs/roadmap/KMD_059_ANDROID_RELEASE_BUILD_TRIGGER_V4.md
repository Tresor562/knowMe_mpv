# KMD-059 Android standalone release build trigger v4

Fresh PR trigger for the standalone Android release APK after the Expo monorepo entrypoint and internal workspace contract build fixes. The APK must embed its JavaScript bundle and start without Metro.

Build trigger revision: 3.
