{
  "name": "knowme",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test && node --test scripts/release-preflight.test.mjs",
    "format": "prettier --write .",
    "db:generate": "pnpm --filter @knowme/api prisma:generate",
    "db:migrate": "pnpm --filter @knowme/api prisma:migrate",
    "check:project": "node scripts/check-project.mjs",
    "check:release": "node scripts/release-preflight.mjs"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.5",
    "typescript": "^5.8.3"
  }
}